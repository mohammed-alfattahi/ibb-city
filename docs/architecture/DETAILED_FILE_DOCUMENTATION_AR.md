# 📄 التوثيق التفصيلي للملفات (Detailed File Documentation)

توثيق شامل لجميع ملفات المشروع، موضحاً الغرض من كل ملف والمكونات الرئيسية بداخله.

---

## 👥 تطبيق المستخدمين (`users/`)

يدير هذا التطبيق كل ما يتعلق بالمستخدمين، المصادقة، الصلاحيات، وملفات الشركاء.

### 1. النماذج (`models.py`)

- **الغرض**: تعريف جداول قاعدة البيانات الخاصة بالمستخدمين.
- **الكلاسات الرئيسية**:
  - `User`: توسيع لنموذج Django User ليشمل حقول إضافية (رقم الهاتف، الصورة، الدور).
  - `PartnerProfile`: ملف الشريك التجاري (السجل التجاري، الحالة).
  - `Role`: إدارة الأدوار والصلاحيات المخصصة.
  - `Interest`: اهتمامات المستخدمين (مطاعم، فنادق، إلخ).
  - `JobPosition`: المسميات الوظيفية (للشركات).

### 2. العروض (`views*`)

- **`views.py`**: العروض العامة للمستخدم (تسجيل، دخول، ملف شخصي).
  - `VisitorSignUpView`: تسجيل سائح جديد.
  - `UserProfileView`: عرض وتعديل الملف الشخصي.
  - `UnifiedLoginView`: تسجيل الدخول الموحد.
- **`views_api.py`**: نقاط نهاية API (REST Framework).
  - `RegisterView`: تسجيل مستخدم جديد عبر API.
  - `ProfileView`: قراءة/تحديث الملف الشخصي عبر API.
- **`views_partner.py`**: عروض خاصة بلوحة تحكم الشريك.
  - `PartnerSignUpView`: تسجيل شريك جديد.
  - `PartnerDashboardView`: الصفحة الرئيسية للوحة الشريك.
- **`views_partner_upgrade.py`**: منطق ترقية الحساب من مستخدم عادي إلى شريك.

### 3. النماذج (`forms*`)

- **`forms.py`**: النماذج العامة.
- **`forms_auth.py`**: نماذج التسجيل (مقسمة لزائر وشريك).
  - `VisitorSignUpForm`
  - `PartnerSignUpForm`
- **`forms_login.py`**: نموذج تسجيل الدخول المخصص.
- **`forms_update.py`**: نماذج تحديث البيانات.

### 4. الإدارة (`admin.py`)

- **الغرض**: تخصيص واجهة Django Admin.
- **المميزات**:
  - فلاتر مخصصة (تصفية حسب نوع المستخدم).
  - إجراءات مخصصة (تفعيل الحسابات، الموافقة على الشركاء).
  - `UserAdmin`: تخصيص عرض جدول المستخدمين.

### 5. الخدمات والأدوات

- **`email_service.py`**: منطق إرسال رسائل البريد الإلكتروني (الترحيب، التحقق، استعادة كلمة المرور).
- **`backends.py`**: `EmailOrUsernameModelBackend` يسمح بتسجيل الدخول باستخدام البريد الإلكتروني أو اسم المستخدم.
- **`mixins.py`**:
  - `PartnerRequiredMixin`: يمنع غير الشركاء من الوصول لصفحات معينة.
  - `ProfileCompleteRequiredMixin`: يفرض إكمال الملف الشخصي.
- **`signals.py`**:
  - إنشاء `PartnerProfile` تلقائي عند تسجيل شريك.
  - إرسال إشعار ترحيبي عند التسجيل.
- **`serializers.py`**: تحويل بيانات النماذج إلى JSON لاستخدامها في الـ API.

---

## 📍 تطبيق الأماكن (`places/`)

يختص بإدارة البيانات الجغرافية والسياحية، بما في ذلك المنشآت التجارية والمعالم.

### 1. النماذج (`models/`)

- **`base.py`**: النموذج الأساسي `Place` الذي ترث منه جميع أنواع الأماكن، ويحتوي على البيانات المشتركة (الاسم، الموقع، الوصف).
- **`establishments.py`**:
  - `Establishment`: المنشآت التجارية (فنادق، مطاعم) مع حقول إضافية (المالك، ساعات العمل).
  - `EstablishmentUnit`: الوحدات القابلة للحجز داخل المنشأة (غرف، طاولات).
- **`landmarks.py`**: `Landmark` للمعالم التاريخية والطبيعية.
- **`routes.py`**: `TouristRoute` للمسارات السياحية المقترحة.

### 2. العروض (`views*`)

- **`views_public.py`**: العروض العامة للزوار.
  - `PlaceListView`: عرض وتصفية الأماكن.
  - `PlaceDetailView`: صفحة تفاصيل المكان.
  - `CategoryPlaceListView`: عرض الأماكن حسب الفئة.
- **`views_partner.py`**: إدارة المنشآت من قبل الشركاء.
  - `EstablishmentCreate/Update/DeleteView`: عمليات CRUD للمنشآت.
  - `UnitManagementView`: إدارة الوحدات.
- **`views_partner_contacts.py`**: إدارة جهات الاتصال الخاصة بالمنشأة.
- **`views_wizard.py`**: معالج (Wizard) خطوة بخطوة لإضافة منشأة جديدة.

### 3. النماذج (`forms*`)

- **`forms.py`**: النماذج الأساسية لإضافة وتعديل الأماكن.
- **`forms_wizard.py`**: نماذج خاصة بمعالج الإضافة (مقسمة لخطوات: المعلومات الأساسية، الموقع، الوسائط).

### 4. الخدمات والمحددات

- **`selectors.py`**: دوال لاسترجاع البيانات (Getters) مع منطق تصفية معقد.
  - `get_places_list`: استرجاع قائمة الأماكن مع الفلاتر.
  - `get_place_detail`: استرجاع تفاصيل مكان واحد.
- **`services/`**: منطق الأعمال (Business Logic).
  - `place_service.py`: إنشاء وتحديث الأماكن.
  - `geo_service.py`: حساب المسافات والتعامل مع الإحداثيات.

### 5. ملفات أخرى

- **`filters.py`**: فلاتر البحث المتقدمة (باستخدام `django-filter`).
- **`context_processors.py`**: `partner_context` يضيف بيانات الشريك (مثل عدد المنشآت) إلى القوالب.
- **`admin.py`**: تخصيص لوحة التحكم (خرائط Leaflet، عرض الصور المصغرة).

---

## 💬 تطبيق التفاعلات (`interactions/`)

يدير التفاعلات الاجتماعية، التقييمات، الإشعارات، والخدمات المتقدمة (مثل البث المباشر للموقع).

### 1. النماذج (`models/`)

- **`reviews.py`**: تقييمات الأماكن (`Review`) والتعليقات.
- **`notifications.py`**: تخزين الإشعارات (`Notification`) وإعدادات التنبيهات.
- **`favorites.py`**: الأماكن المفضلة (`Favorite`) وجداول الرحلات (`Itinerary`).
- **`reports.py`**: نظام البلاغات (`Report`) عن المحتوى المخالف.

### 2. العروض (`views*`)

- **`views_public.py`**: إضافة التعليقات، الإبلاغ، والمفضلة.
- **`views_itinerary.py`**: إدارة جداول الرحلات (إنشاء، تعديل، مشاركة).
- **`views_liveshare.py`**: تتبع الموقع المباشر (Live Location Sharing).
- **`views_preferences.py`**: إعدادات التنبيهات للمستخدم.

### 3. نظام الإشعارات (`notifications/`)

- **`dispatcher.py`**: الموزع المركزي للإشعارات (يقرر القناة المناسبة: ويب، تطبيق، إيميل).
- **`handlers.py`**: معالجات لأنواع محددة من الأحداث (مثل: `NewReviewHandler`).
- **`onesignal_service.py`**: التكامل مع خدمة OneSignal لإرسال Web Push.
- **`firebase_service.py`**: التكامل مع Firebase (FCM).

### 4. المهام الخلفية (`tasks/`)

- **`send_notifications`**: مهام Celery لإرسال الإشعارات بشكل غير متزامن (Asynchronous).

### 5. ملفات أخرى

- **`signals.py`**: إطلاق الإشعارات تلقائياً عند وقوع أحداث (مثل: إنشاء تقييم جديد).
- **`context_processors.py`**: `notification_context` يضيف عدد الإشعارات غير المقروءة للشريط العلوي.

---

## ⚙️ تطبيق الإدارة (`management/`)

تطبيق شامل لإدارة النظام، الإعلانات، المحتوى، والطلبات.

### 1. النماذج (`models/`)

- **`advertisements.py`**: إدارة الإعلانات (`Advertisement`) وأنواعها (بنر، مميز).
- **`requests.py`**: طلبات الموافقات (ترقية شريك، إضافة منشأة).
- **`alerts.py`**: تنبيهات الطوارئ (`EmergencyAlert`) وأرقام الطوارئ.
- **`content.py`**: إدارة محتوى الموقع (الشرائح، الإعدادات، القوائم).
- **`audit.py`**: سجلات التدقيق (`AuditLog`) لتتبع إجراءات المشرفين.

### 2. العروض (`views*`)

- **`views_admin.py`**: لوحة التحكم الرئيسية (Dashboard)، البحث الشامل، والإحصائيات.
- **`views_approvals.py`**: معالجة طلبات الموافقة والرفض.
- **`views_ads.py`**: إدارة الإعلانات (إنشاء، تعديل، إحصائيات).
- **`views_cms.py`**: عروض إدارة المحتوى (الصفحات، المناطق).

### 3. أدوات الإدارة (`admin_*.py`)

- **`admin_dashboard.py`**: منطق تجميع ويدجت لوحة القيادة.
- **`admin_actions.py`**: الإجراءات المجمعة (Bulk Actions) في Django Admin.
- **`admin_ui.py`**: تخصيص واجهة المستخدم للإدارة (Jazzmin Tweaks).

### 4. الخدمات (`services/`)

- **`analytics_service.py`**: تجميع تحليلات النظام (الزيارات، النقرات).
- **`settings_service.py`**: إدارة إعدادات النظام مع التخزين المؤقت (Caching).
- **`export_service.py`**: تصدير البيانات (CSV, Excel).

### 5. أخرى

- **`context_processors.py`**: `site_ui` يمرر القوائم والإعدادات العامة لكل القوالب.
- **`importers.py`**: استيراد البيانات الأولية (Fixtures).

---

## 🎉 تطبيق الفعاليات (`events/`)

إدارة المهرجانات، المواسم، والفعاليات الثقافية.

### 1. الملفات الرئيسية

- **`models.py`**:
  - `Event`: تعريف الفعالية (الاسم، تاريخ البدء/الانتهاء، المكان، السعر).
- **`views.py`**:
  - `EventListView`: عرض قائمة الفعاليات القادمة.
  - `EventDetailView`: صفحة تفاصيل الفعالية.
- **`admin.py`**: إدارة الفعاليات من لوحة التحكم، مع إمكانية تكرار الفعاليات (Duplicate Action).
- **`urls.py`**: مسارات الفعاليات (`/events/`, `/events/<slug>/`).

---

## 🎨 نظام إدارة المحتوى (`cms/`)

نظام مرن للتحكم في واجهة المستخدم (User Interface Control) دون الحاجة لتعديل الكود.

### 1. النماذج (`models.py`)

- **`UIZone`**: تعريف مناطق في الصفحة (مثل: `homepage_hero`, `sidebar_top`).
- **`UIComponent`**: مكونات قابلة لإعادة الاستخدام (مثل: `Banner`, `CardGrid`).
- **`ZoneComponent`**: جدول وسيط يربط المكونات بالمناطق مع تحديد الترتيب والظهور.

### 2. الأدوات

- **`templatetags/cms_tags.py`**:
  - `{% render_zone 'zone_name' %}`: تاج مخصص لاستدعاء وعرض المنطقة في القوالب.
- **`services/renderer.py`**: منطق معالجة المكونات وتحويلها إلى HTML.
- **`admin.py`**: واجهة سحب وإفلات (Drag & Drop) لترتيب المكونات داخل المناطق.

---

## 🔄 العلاقات بين الملفات (File Relationships & Data Flow)

كيف تتفاعل هذه الملفات مع بعضها البعض لبناء النظام المتكامل.

### 1. دورة حياة الطلب (Request Lifecycle)

1. **`urls.py`**: يستقبل الرابط من المتصفح (مثلاً: `/places/1/`).
2. **`views.py`**: يعالج الطلب (مثلاً: `PlaceDetailView`).
3. **`selectors.py`**: (اختياري) يستدعي الدالّة لجلب البيانات من قاعدة البيانات.
4. **`models.py`**: يمثل البيانات المسترجعة (جدول `Place`).
5. **`templates/`**: يتم تمرير البيانات إلى القالب لعرضها للمستخدم.

### 2. العلاقات بين التطبيقات

- **`users` ↔ `places`**:
  - `Place.owner` يرتبط بـ `User` (العلاقة: الملكية).
  - عند تسجيل شريك في `users`، يتم توجيهه لإضافة منشأة في `places`.
- **`interactions` ↔ `places`**:
  - `Review.place` يربط التقييم بالمكان.
  - `Favorite.place` يربط المفضلة بالمكان.
- **`management` ↔ `all`**:
  - `management` يشرف على محتوى جميع التطبيقات الأخرى (موافقة على المنشآت، إدارة المستخدمين).

### 3. الخدمات الخلفية

- **`signals.py`** في `interactions` يستمع للتغييرات في `models.py` في `reviews`.
- عند حفظ تقييم جديد -> يطلق الـ Signal -> ينشئ `Notification` في `interactions`.
- يتم إرسال الإشعار عبر `onesignal_service.py`.

---
