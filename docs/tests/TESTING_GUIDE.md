# 🧪 دليل اختبار المشروع - دليل إب السياحي

هذا الدليل مخصص لفريق التطوير وضمان الجودة (QA) لضمان جودة واستقرار منصة **دليل إب السياحي** بعد النشر. استخدم هذه الإجراءات للتحقق من الميزات، واختبار التغييرات الجديدة، وصيانة النظام.

---

## 1. الاختبارات المؤتمتة (Backend)

يستخدم المشروع إطار عمل اختبار Django و `pytest` لاختبارات الوحدة والتكامل.

### تشغيل الاختبارات الأساسية
لتشغيل جميع الاختبارات في المشروع:
```powershell
python manage.py test
```

### تشغيل اختبارات تطبيق معين
للتركيز على وحدة معينة (مثل `places` أو `users`):
```powershell
python manage.py test places
python manage.py test users
```

### استخدام Pytest (متقدم)
`pytest` متاح لتقارير أكثر تفصيلاً وتنفيذ أسرع:
```powershell
pytest
```

---

## 2. سكربتات التحقق من البيئة

لدينا مجموعة من سكربتات التحقق المخصصة في المجلد الرئيسي ومجلد `scripts/`. هذه هي "اختبارات سريعة" (smoke tests) تتحقق من منطق النظام المحدد دون الحاجة إلى متصفح كامل.

### أوامر التحقق الرئيسية
| السكربت | النطاق | الأمر |
| :--- | :--- | :--- |
| **النظام الكامل** | يتحقق من جميع المكونات الأساسية | `python verify_full_system.py` |
| **الأماكن/الخريطة** | يتحقق من إنشاء الأماكن والمنطق الجغرافي | `python verify_places.py` |
| **تدفق الإعلانات** | يتحقق من تقديم الإعلانات والموافقة عليها | `python verify_ad_flow.py` |
| **الإشعارات** | يتحقق من المحفزات وعدد الرسائل غير المقروءة | `python scripts/verify_all_notifications.py` |
| **الفلاتر** | يتحقق من البحث وتصفية الفئات | `python verify_filters.py` |
| **التحليلات** | يتحقق من تتبع المشاهدات والإحصائيات اليومية | `python scripts/verify_analytics.py` |

> [!TIP]
> قم دائماً بتشغيل `python verify_full_system.py` بعد أي عملية نشر جديدة للتأكد من أن قاعدة البيانات والإعدادات الحرجة تعمل بشكل صحيح.

---

## 3. الاختبار اليدوي لواجهة المستخدم (التدفقات الرئيسية)

يضمن الاختبار اليدوي أن تجربة المستخدم (UX) صحيحة وأن القوالب تظهر بشكل سليم. قم بتنفيذ هذه الاختبارات بالترتيب المذكور.

### التدفق (أ): تجربة السائح
1. **الصفحة الرئيسية**: تأكد من عمل شريط البحث وأن الفئات تؤدي إلى قائمة مصفاة بشكل صحيح.
2. **قائمة الأماكن**: تحقق مما إذا كان "شريط التصفية الجانبي" يقوم بتصفية الأماكن حسب الفئة/التقييم بشكل صحيح.
3. **تفاصيل المكان**: تأكد من أن عرض الصور والخرائط و"الشريط الجانبي للمعلومات" يعرض البيانات بشكل صحيح.
4. **التقييمات**: أضف تقييماً (كاسم مستخدم مسجل) وتأكد من ظهوره بعد الموافقة.

### التدفق (ب): لوحة تحكم الشريك
1. **تسجيل الدخول**: قم بالتسجيل/تسجيل الدخول كحساب "شريك".
2. **إضافة مكان**: أضف منشأة جديدة (فندق/مطعم). تحقق من رفع الصور.
3. **مركز الإعلانات**: قم بتقديم طلب "تعزيز" (Boost) لمكان وتحقق من مقاييس "الوصول".
4. **التحليلات**: تحقق مما إذا كانت "بطاقات المؤشرات" (المشاهدات/التقييمات) تتحدث عند تفاعل الناس مع المكان.

### التدفق (ج): إشراف المدير (Admin)
1. **لوحة إدارة Django**: الدخول إلى `/admin/`.
2. **طلبات الموافقة**: الموافقة على مكان أو إعلان معلق.
3. **الإشعارات**: تأكد من تلقي الشريك إشعاراً عند الموافقة على مكانه.

---

## 4. تكامل تطبيق الموبايل (اختبار API)

بما أن الواجهة الأمامية هي تطبيق Flutter، يجب علينا التحقق من نقاط نهاية API.

- **التوثيق**: ادخل إلى `/api/` لتصفح نقاط النهاية المتاحة.
- **الأدوات**: استخدم **Postman** أو **Insomnia** لاختبار:
    - `POST /api/token/`: تسجيل الدخول والحصول على JWT.
    - `GET /api/places/`: التحقق من هيكل JSON لقوائم الأماكن.
    - `POST /api/reviews/`: التأكد من أن الهيدرز تتضمن `Authorization: Bearer <token>`.

---

## 5. أفضل ممارسات الاختبار

- **الاختبار في متصفح محايد**: استخدم **وضع التصفح المتخفي** لاختبارات واجهة المستخدم اليدوية لتجنب مشاكل الكاش أو الجلسات.
- **محاكاة الـ API الخارجية**: إذا كنت تختبر الطقس أو الخرائط محلياً، تأكد من ضبط مفاتيح الـ API في ملف `.env`.
- **تحقق من السجلات (Logs)**: إذا فشل الاختبار، انظر إلى مخرجات الطرفية (terminal) أو مجلد `logs/`.
- **الإبلاغ عن الأخطاء**: استخدم GitHub Issues أو لوحة مشروع الفريق. قم بتضمين:
    - 📍 **منطقة الميزة**: (مثلاً: الصفحة الرئيسية، لوحة الإدارة)
    - 📝 **خطوات إعادة الإنتاج**: (مثلاً: 1. تسجيل الدخول -> 2. الضغط على X -> 3. خطأ Y)
    - 🖼️ **لقطات شاشة**: خاصة لمشاكل محاذاة واجهة المستخدم.

---

*آخر تحديث: يناير 2026*
